# Frontend telemetry (Vite — rebuild required)
VITE_TELEMETRY_ENABLED=true
VITE_AB_VARIANT=treatment

# Discord Activity — same Client ID as backend DISCORD_CLIENT_ID
# VITE_DISCORD_CLIENT_ID=your_app_client_id

# Public dev URL (informational; tunnel + nginx serve this)
# VITE_PUBLIC_URL=https://local.sweeptogether.com
