/** Must match backend `CHUNK_SIZE`. */
export const CHUNK_SIZE = 32;

/** Logical cell size in CSS pixels at scale 1. */
export const BASE_CELL_PX = 28;

export const CHUNK_BUFFER = 3;
export const CHUNK_DIRECTION_EXTRA = 1;
export const CHUNK_SUBSCRIBE_DEBOUNCE_MS = 300;

export const MIN_SCALE = 0.25;
export const MAX_SCALE = 4;
